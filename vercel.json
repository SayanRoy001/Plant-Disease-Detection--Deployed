{
  "version": 2,
  "ignoreCommand": "echo Using root for multi-project; backend not built here",
  "builds": [],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://your-backend-domain.example.com/$1"
    }
  ],
  "comments": "Replace destination with real backend URL or remove rewrites if not using proxy."
}
